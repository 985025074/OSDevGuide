# := 和 = 
VAR ?= value：仅当 VAR 未定义时才赋值（条件赋值）。
VAR += more：追加；如果 VAR 已存在，追加方式保留原有类型（如果原来是 := 即即时追加后的结果；若原来是 = 则仍为递归）。若原来未定义，+= 相当于 =（递归）。
override / export 等：控制覆盖和导出到子 Make 进程，与展开类型无关。
VAR = value：延迟展开（recursive），右侧在每次引用 $(VAR) 时再展开。适合需要引用后来才定义的变量或希望每次使用时重新计算的情况。


